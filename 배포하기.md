# 음성 비서 프로그램 배포하기 (책 3.11절)

## 업로드할 파일 — 딱 3개

```
ch03_voicebot.py
requirements.txt
packages.txt
```

> ⚠️ **절대 올리지 말 것**
> - `ch03_env/` — 가상환경. 수백 MB이고 윈도우 전용이라 클라우드에서 못 씁니다.
> - `음성비서만들기_진짜 챗 GPT API 활용법.pdf` — 교재는 저작물입니다. 공개 저장소에 올리면 안 됩니다.
> - `ch03_whisper_example.py` — 여기 API 키를 적었다면 그대로 공개됩니다. `.gitignore`에 넣어뒀습니다.

API 키는 코드에 없고 **앱 사이드바에서 입력받는 방식**이라, 위 3개 파일에는 비밀정보가 없습니다.

> 이 앱은 **Gemini**를 씁니다. API 키는 https://aistudio.google.com/apikey 에서 무료로 발급됩니다.

---

## 1단계 — 깃허브 리포지토리 만들기

1. https://github.com 로그인 (계정 없으면 회원가입)
2. 우측 상단 **+** → **New repository**
3. Repository name: `voicebot`
4. **Public** 선택 (Streamlit 무료 배포는 Public이 가장 간단합니다)
5. **Create repository**

## 2단계 — 파일 업로드

1. 새로 만든 리포지토리 화면에서 **uploading an existing file** 링크 클릭
2. 탐색기에서 위 3개 파일을 끌어다 놓기
3. 아래 **Commit changes** 클릭

## 3단계 — Streamlit Cloud 연동

1. https://share.streamlit.io 접속 → **Continue with GitHub** 으로 가입/로그인
2. 권한 승인 (Authorize)
3. **Create app** → **Deploy a public app from GitHub**
4. 입력값:
   - Repository: `본인아이디/voicebot`
   - Branch: `main`
   - Main file path: `ch03_voicebot.py`
5. **Advanced settings** 클릭 → **Python version: 3.12** 선택 ← 중요
6. **Deploy!**

빌드에 3~7분 걸립니다. `packages.txt` 덕분에 서버에 ffmpeg가 자동 설치됩니다.

---

## 배포 후

앱 URL은 `https://<앱이름>.streamlit.app` 형태로 나옵니다.

사이드바에 Gemini API 키를 넣으면 바로 동작합니다. Gemini는 무료 사용량이 있어서 결제 등록 없이도 쓸 수 있습니다.

## 막혔을 때

| 증상 | 원인 / 해결 |
|---|---|
| 빌드 로그에 `ffmpeg not found` | `packages.txt`를 안 올렸거나 파일명 오타 |
| `ModuleNotFoundError: audioop` | Advanced settings에서 Python 3.12를 안 골랐을 때. 3.13이어도 `requirements.txt`의 `audioop-lts` 줄이 처리해줍니다 |
| 녹음 버튼이 안 보임 | 브라우저 마이크 권한 허용 필요. 배포본은 HTTPS라 보통 잘 됩니다 |
| 답변 소리가 안 남 | 브라우저 자동재생 차단. 사이트 설정 → 소리 → 허용 |
| 코드 고친 뒤 반영이 안 됨 | 깃허브에 다시 올리면 자동 재배포됩니다 |

## 무료 한도

Streamlit Community Cloud는 무료로 앱을 배포할 수 있고, 일정 시간 접속이 없으면 잠자기 상태로 전환됩니다. 다시 접속하면 깨어납니다.
